#include <iostream>
#include <vector>
#include <cmath>
using namespace std;
typedef unsigned long long ull;
const int MAX = 200001;
ull ac[MAX][20];
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int N, root;
    cin >> N >> root;
    return 0;
}