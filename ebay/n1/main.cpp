#include <string>
#include <vector>
#include <queue>
using namespace std;

int solution(int N, vector<vector<int>> simulation_data) {
    int answer = 0;
    vector<pair<int,int>> bank(N);
    vector <bool
    for(int i = 0 ; i < simulation_data.size() ; i++) {

    }

    return answer;
}